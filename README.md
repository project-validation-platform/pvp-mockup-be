# Data Files
Put data files in the `data` directory (create if needed).
Currently, there should be a folder with name `mimic_III` in the data directory, and it should contain a folder `synthetic` to represent any synthetically generated data.
For testing, we can use `ADMISSIONS.csv` for now.
